# CustusX GitLab CI/CD configuration file

default:
  #image: olevs/custusx-build-ubuntu2004 #Prebuilt CustusX, Dockerhub
  #image: olevs/custusx-ubuntu2004 #Empty image, with only environment
  #image: gitlab.sintef.no:5050/custusx/custusx/custusx-ubuntu2004 # Slower build if not reusing the build from last run
  image: gitlab.sintef.no:5050/custusx/custusx/build-ubuntu2004:25.02

stages:          # List of stages for jobs, and their order of execution
  - build
  - deploy

# Cleanup old builds from runners
# Can be removed after a while
cleanup-old-builds:
  stage: .pre
  script:
  - cd $CI_PROJECT_DIR
  - cd ..
  - rm -rf build_Release
  - cd ..
  - ls
  - rm -rf CTK
  - rm -rf IGSTK
  - rm -rf OpenCLUtilityLibrary
  - rm -rf OpenCV
  - rm -rf OpenIGTLink
  - rm -rf OpenIGTLinkIO
  - rm -rf QHttpServer
  - rm -rf VTK
  - rm -rf ITK
  - rm -rf eigen
  - rm -rf medtekAI
  - rm -rf ISB_DataStreaming
  - rm -rf CX
  - rm -rf CS
  - rm -rf FX
  - rm -rf Installed
  - rm -rf temp

.build-job:       # This job runs in the build stage, which runs first.
  stage: build
  variables:
    VERSION: ${CI_COMMIT_SHORT_SHA}
    OS: OS
    SOURCE_DIR: $CI_PROJECT_DIR/$OS/cx/cx
  script:
    # - export # For listing all gitlab variables
    - echo "Compiling the code..."
    - cd $CI_PROJECT_DIR
    - echo $OS
    - qmake --version
    #- echo "Qt version of libigtlioGUI.so, libCTKCore.so, and libcxGui.so:"
    #- strings ../../OpenIGTLinkIO/build_Release/lib/libigtlioGUI.so | grep "Qt_5\."
    #- strings ../../CTK/build_Release/CTK-build/bin/libCTKCore.so | grep "Qt_5\."
    #- strings ../build_Release/bin/libcxGui.so | grep "Qt_5\."
    - git clone https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.sintef.no/custusx/CustusX.git $SOURCE_DIR
    - cd $SOURCE_DIR
    - git checkout $CI_COMMIT_BRANCH
    - ./install/cxJenkinsMasterBuild.py --create_package --developer_doc --main_branch $CI_COMMIT_BRANCH --gitrepo_main_site_base https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.sintef.no/custusx
    # Don't work. Probably needs to rename file in .py scripts instead
    #- mv package_artefacts/CustusX*.tar.gz package_artefacts/CustusX_$VERSION_$OS.tar.gz
    - echo "Compile complete."
    - echo "Running unit tests... "
    - xvfb-run ./install/cxJenkinsMasterBuild.py --test_package --main_branch $CI_COMMIT_BRANCH --gitrepo_main_site_base https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.sintef.no/custusx
  artifacts:
    when: always
    name: ${CI_JOB_NAME}-${CI_COMMIT_REF_NAME}-${CI_COMMIT_SHA}
    paths:
    # CI_PROJECT_DIR="/builds/custusx/CustusX"
      - package_artefacts/$OS/CustusX*.tar.gz
      - package_artefacts/html_pure/
      - package_artefacts/html_dev/
      - test_results/*junit.xml
    reports:
      junit: test_results/*junit.xml
    #expire_in: never # Default is 30 days. May need to set releases to "never"?

build-and-test-ubuntu20:
  extends: .build-job
  image: gitlab.sintef.no:5050/custusx/custusx/custusx-build-ubuntu2004:25.02
  variables:
    OS: Ubuntu2004

build-and-test-ubuntu22:
  extends: .build-job
  image: gitlab.sintef.no:5050/custusx/custusx/custusx-build-ubuntu2204:25.02
  variables:
    OS: Ubuntu2204

.deploy:
  stage: deploy
  environment: production
  variables:
    VERSION: ${CI_COMMIT_SHORT_SHA}
  script:
  - echo "Deploying application..."
  - cd $CI_PROJECT_DIR/package_artefacts/Ubuntu2004
  - INSTALLER_FILE=$(find CustusX*.tar.gz)
  - 'curl
     --header "JOB-TOKEN: $CI_JOB_TOKEN"
     --upload-file $INSTALLER_FILE
     "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/CustusS/$VERSION/Ubuntu2004/$INSTALLER_FILE"'
  - cd $CI_PROJECT_DIR/package_artefacts/Ubuntu2204
  - INSTALLER_FILE=$(find CustusX*.tar.gz)
  - 'curl
     --header "JOB-TOKEN: $CI_JOB_TOKEN"
     --upload-file $INSTALLER_FILE
     "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/CustusS/$VERSION/Ubuntu2204/$INSTALLER_FILE"'
#  - cd $CI_PROJECT_DIR/package_artefacts
#  - INSTALLER_FILES=$(find CustusX*.tar.gz)
#  - echo $INSTALLER_FILES
#  - |
#    for file in $INSTALLER_FILES; do
#      'curl
#       --header "JOB-TOKEN: $CI_JOB_TOKEN"
#       --upload-file $file
#       "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/CustusX/$VERSION/$file"'
#    done

deploy-commmit:
  extends: .deploy
  variables:
    VERSION: ${CI_COMMIT_SHORT_SHA}
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH != $CI_DEFAULT_BRANCH # Only run for changes to the default (develop) branch
      # when: never
    - if: '$CI_COMMIT_TAG == null' # Run for all commits, except tags

deploy-tag:
  extends: .deploy
  variables:
    VERSION: ${CI_COMMIT_TAG}
  rules:
    - if: '$CI_COMMIT_TAG != null' # Run if a tag is present (= release or release candidate)

pages:
  stage: deploy
  environment: production
  script:
    - cd $CI_PROJECT_DIR
    - mv package_artefacts/html_pure/ public/user_doc/
    - mv package_artefacts/html_dev/ public/developer_doc/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH # Run for all changes to the default (develop) branch only
